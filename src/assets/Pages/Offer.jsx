import { Link } from "react-router-dom";


const Offer = ({ }) => {
    const { id } = useParams();
    return (
        <div>

            {/* <h1>Je suis sur la page de l'offre dont l'id est {id}</h1>; */}

        </div>
    );
};

export default Offer;